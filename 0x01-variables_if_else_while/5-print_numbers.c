#include <stdio.h>
/**
 * main - This code will print 10 base numbers
 * starting with 0, followed by
 * a new line
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a = 0;
	while (a <= 10)
	{
		printf("Value of a: %d\n", a);
		a++;
	}
	return (0);
}
